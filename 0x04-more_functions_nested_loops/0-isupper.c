#include "main"
#include <stdio.h>

/**
 * _islower - Description: A C program that print
 *
 * @c: input
 *
 * Return: 1 if c is lowercase - 0 somthind else
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	return (0);
}
