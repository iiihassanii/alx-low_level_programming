#include "main.h"
#include <ctype.h>

/**
 * _abs - Description: A C program that print
 *
 * @n: input
 *
 * Return: 1 if c is letter, lowercase or uppercase - 0 otherwise
 */

int _abs(int n)
{
	if (n < 0)
		n *= -1;
	return (n);
}
