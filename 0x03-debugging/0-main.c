#include "main.h"
#include <stdio.h>


/**
 * main - Entry point
 *
 * Description: A C program that print
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	if (positive_or_negative(0) == "0 is zero")
		printf("0 is zero");
}
